# Required: Solana RPC endpoint (e.g., Alchemy, QuickNode, or public endpoint)
RPC_ENDPOINT=https://solana-mainnet.g.alchemy.com/v2/rHh9-faE9mkIfUP3TRiTW

# Optional: Shredstream endpoint (defaults to public endpoint if not set)
# SHREDSTREAM_ENDPOINT=shredstream.mainnet-beta.jito.wtf:10000

# Optional: WebSocket port (defaults to 8080)
# WS_PORT=8080
